import os
# import sys
# import io
# sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
from public.common.readconfig import ReadConfig

# nickName_Input
nickName_Input = 'name->username'

# email_Input
email_Input = 'name->email'

# sendCode_Button
sendCode_Button = 'tag_name->button'

# vCode_Input
vCode_Input = 'name->vcode'

# SendVCodeSuccess
SendVCodeSuccess = 'xpath->//*[contains(text(),发送成功)]'

# agreeCluse
agreeCluse = 'css->div[class^=checkContent_]>div'

# nextStep_Button
nextStep_Button = 'link_text->下一步'

# password_Input
password_Input = 'name->password'

# passwordConfirm_Input
passwordConfirm_Input = 'name->passwordConfirm'

# ImageCode
ImageCode = 'css->div[class^=warp]>div>span'

# imageCode_Input
imageCode_Input = 'name->imageCode'

# register_Button
register_Button = 'tag_name->button'

